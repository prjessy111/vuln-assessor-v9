# 신규 서버 연동 체크리스트 (다음엔 이것만 보면 됨)

> **목적:** `servers.csv` 에 서버를 새로 넣은 뒤, Windows·Linux 각각 **딱 뭘 더 해줘야 원격 점검이 되는지** 한 장으로 정리.
> 상세 Windows WinRM 절차는 [`사전작업.md`](사전작업.md) 참고. 이 문서는 "순서·핵심"만.

---

## 0. 한눈에 (요약표)

| 대상 | 접속 | 컨트롤러(앱 PC)에서 할 일 | 대상 서버에서 할 일 |
|---|---|---|---|
| **Windows** | WinRM / TCP **5985** | `TrustedHosts` 에 대상 IP 등록 (1회) | WinRM 켜짐 + 방화벽 5985 인바운드 허용 |
| **Linux** | SSH / TCP **22** | (없음 — ssh 클라만 있으면 됨) | sshd 기동 + 계정 로그인 허용 + (파일점검이면) sudo |

두 경우 모두 **servers.csv 등록 → 위 연동 → 앱 재시작 → 점검** 순서.

---

## 1. 공통 1단계 — servers.csv 등록

```
# hostname,ip,os,username,password[,asset_no][,server_id]
jessy107,192.168.159.107,windows,Administrator,omni007!,SVR-2026-0031,60
jessy207,192.168.159.207,linux,root,1q2w3e4r!,SVR-2026-0030,50
```

- `os` 는 `windows` / `linux` 로 정확히. (Windows→WinRM, Linux→SSH 자동 분기)
- CSV 는 **CP949(한글 헤더 보존)** — 편집 시 인코딩 깨지지 않게 주의.
- `servers.csv` 는 **평문 비번 포함 → 커밋 금지** (`.gitignore` 등록됨).

---

## 2A. Windows 추가 작업 (WinRM)

접속 방식 = **컨트롤러에서 네이티브 `Invoke-Command`(Negotiate)** → 대상의 5985 로 붙는다.

### 컨트롤러(앱 도는 PC) — 관리자 PowerShell 에서 1회

```powershell
Start-Service WinRM; Set-Service WinRM -StartupType Automatic
Set-Item WSMan:\localhost\Client\TrustedHosts -Value "192.168.159.107" -Concatenate -Force
Restart-Service WinRM
Test-NetConnection 192.168.159.107 -Port 5985      # TcpTestSucceeded : True 확인
```
여러 대면 `-Value "IP1,IP2"`. `-Concatenate` 라 기존 등록 유지.

### 대상 서버 — 5985 가 안 열려 있으면 (Test 가 False)

대상에 RDP 로 들어가거나, 컨트롤러에서 SMB(445)로 원격으로 켠다:
```powershell
# 대상에서 직접:
Enable-PSRemoting -Force
# (방화벽) WinRM-HTTP-In 인바운드 5985 허용 확인
```

### 접속 검증 (컨트롤러에서)
```powershell
$cred = Get-Credential           # Administrator / (csv 비번)
Invoke-Command -ComputerName 192.168.159.107 -Credential $cred -ScriptBlock { hostname; whoami }
```
→ 대상 hostname + `대상\administrator` 나오면 성공.

---

## 2B. Linux 추가 작업 (SSH)

접속 방식 = **SSH 22** (비번 또는 키).

### 대상 서버에서 확인
- `sshd` 기동 중, 방화벽 22 허용.
- csv 의 계정으로 로그인 가능.
- **파일 산출형 점검(결과파일 생성)** 이면 해당 계정이 `sudo` 가능해야 할 수 있음 → csv 뒤에 `use_sudo` 서버플래그 또는 점검 옵션으로 처리.

### 접속 검증 (컨트롤러에서)
```bash
ssh root@192.168.159.207 'hostname; id'
```
→ hostname + uid 나오면 성공.

---

## 3. 마무리 (공통)

1. **앱(node) 재시작** — 새 서버/설정 반영 (`npm run mock`).
2. 서버 관리·진단 화면에서 대상 선택 → 수집·진단.
3. 스크립트 즉시 점검(에드혹)·에이전트 점검도 **동일 경로**로 원격 실행됨.

---

## 4. 자주 나는 에러 → 바로 조치

| 증상 | 뜻 | 조치 |
|---|---|---|
| **`WinRM PowerShell execution failed: Failed to process the request, status Code:`** | 포트는 열렸는데 **대상이 이 WinRM 접속을 승인 안 함** (TrustedHosts 누락 / 대상 WinRM 인증정책) | 컨트롤러 **TrustedHosts 등록**(2A) → `Restart-Service WinRM` → 앱 재시작. 그래도면 대상 `Enable-PSRemoting` |
| `TcpTestSucceeded : False` | 대상 WinRM 미기동/방화벽 | 2A "대상 서버" 항목 |
| `Access is denied` (서비스 제어 시) | PowerShell 이 관리자 아님 | 관리자 창으로 재실행 |
| `Access denied` (Invoke-Command 시) | 계정/비번 오류 | csv 비번 재확인 |
| Linux `Permission denied` | 계정/키/비번 오류 | 계정·키 확인, sshd 로그 |
| 결과 파일 못 찾음 | 스크립트가 쓰는 경로가 탐색경로 밖 | 에드혹 등록 시 `result_glob`(예: `C:\ad_audit\*_AD_DC.txt`) 지정 |

> **핵심:** 스크린샷의 `Failed to process the request` = "권한 승인 안 됨" = 대개 **컨트롤러 TrustedHosts 누락**. 107 이 되는 이유가 이 등록이 되어 있어서다. 신규 Windows 서버는 **매번 TrustedHosts 에 IP 추가**가 필수.

---

## 5. 참고 — 코드상 접속 경로 (2026-07 기준, 통일됨)

- **에드혹/에이전트 점검 중 결과파일 산출형**(`result_glob` 지정): `src/agent/pipeline.js` → `runOnTarget` → `scriptDeployService.runDirectScriptDeployment`
  - Windows: 기본 `powershell-only` = **네이티브 Invoke-Command(Negotiate)** — 컨트롤러 TrustedHosts 필요, 대상에 Basic 불필요.
  - Linux: **SSH**.
  - 결과 회수: **base64 청크 다운로드**(`SCRIPT_DEPLOY_WINRM_CHUNK_SIZE`) — 대용량 결과파일(수십 KB+)도 안전. *(예전 stdout `-Raw` 방식의 대용량 유실 문제 해소)*
- **stdout 캡처형**(결과파일 없이 표준출력만): 기존 `scriptRunner` 경로.
- 전송 방식 강제 전환(예외 상황): 환경변수 `SCRIPT_DEPLOY_WINDOWS_WINRM_MODE=nodejs-winrm` 로 Basic(5985) 경로 사용 가능. 이때만 대상에 `Basic`/`AllowUnencrypted` 필요(→ 사전작업.md 부록 B).
