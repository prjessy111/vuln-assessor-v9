# 세션 핸드오프 2026-07-18 — 다국어(i18n)

## 완료

**화면 UI 다국어화 (한국어/영어/일본어/중국어) — 커밋 완료**

- **자체 i18n 모듈** `src/i18n/index.js` (외부 라이브러리 0):
  - `t('네임스페이스.키', {파라미터})` — 로케일 문자열, `{n}` 치환, 폴백(요청언어→ko→키그대로)
  - `td('한국어값')` — DB 저장 상태값(양호/취약/진행중/승인 등) 표시 시점 치환, `_data.json` 사전(en/ja/zh 29키), ko는 원문 반환
  - Express 미들웨어: 언어 결정 우선순위 `?lang=` > `lang` 쿠키(1년) > Accept-Language > ko. `res.locals.t/td/lang/langs/langLabels` 주입
  - server-mock.js 309행 근처 `app.use(i18n.middleware)` 연결
- **로케일**: `src/i18n/locales/{ko,en,ja,zh}/*.json` — ko 2,207키(24 네임스페이스), en/ja/zh 각 2,236키(+_data 29). **4개 언어 키셋 완전 대칭 검증됨**
- **뷰 62개 전부 전환** (9개 그룹 병렬 작업): dashboard/login/diagnosis/collection·collect/vuln·remediation/exceptions·exclusions/servers·users·error/schedules·rulesets/cve·agent·assess/notifications·audit·inspection·security·trends·history/reports 10종
- **언어 선택기**: 헤더 우측 상단, **순수 링크 방식**(`<a href="?lang=xx">`, JS 의존 0). 라벨 = 한글 표기(한국어·영어·일본어·중국어), 현재 언어 파란 강조. `.lang-switch` 스타일 style.css
  - ※ 최초 `<select onchange>` 방식은 "드롭다운 눌러도 무반응" 이슈로 링크로 교체함(값 변경 없으면 change 미발화 등 회피)

**부수 버그 수정 (i18n 무관, 발견하여 수정)**
- `/diagnosis/:id/ai` 전 id에서 500 `classifyPlatform is not defined` → `classifyPlatform`이 `startServer()` **안**에 정의됐는데 호출부(3177 등)는 **밖**이라 안 보임. 정의를 startServer 선언 앞 모듈 스코프로 이동해 해결. (server-mock.js 스코프 함정 재발 사례 — 메모리 [[server-mock-startserver-scope]] 기록)
- `.gitignore`에 `scripts/ai-ready/fsi_config.ini` 추가 — MSSQL sa 평문 비번 노출 파일, 커밋 차단

**검증**: 62뷰 컴파일 OK / 로케일 4언어 대칭 OK / 참조 t()키 전부 존재 / 29라우트×4언어 = 116요청 전부 200, 서버 에러 0 / ko 화면 회귀 없음(각 그룹 렌더 diff)

## 미완 / 향후 (I18N-PLAN.md 3단계+)

- **데이터 내용은 여전히 한국어** (설계상 이번 범위 아님): 진단 항목명(SRV-xxx 제목), LLM 판정 사유문, 수집 로그, 알림 본문, 엑셀 리포트 산출물, 사용자 이름, 정책 고유명사(전자금융기반시설_2026). 언어 바꾸면 화면 틀만 번역되고 데이터는 한국어. → 완전 번역은 LLM 실시간 번역 계층 필요(별도 과제)
- **서버측 생성 문자열 일부 미번역**: 라우트 핸들러가 한국어로 만들어 내려주는 값(일부 flash/alert 메시지, ai_result의 phase_label·resultScope 라벨, `data.overall==='성공'` 클라이언트 표시 등). 뷰 t()로는 못 잡음 → 라우트측 전환 필요
- 날짜/숫자 포맷은 현행 유지(`toLocaleString('ko-KR')` 등 일부 ko 고정)

## 참고
- 실행: `npm run mock` → http://localhost:3000 (테스트는 PORT=3099 사용했음)
- **PC에 `pkill` 없음**(Git Bash) → 서버 재기동 시 이전 node 프로세스가 안 죽어 옛 코드에 물리는 함정. PowerShell `Get-CimInstance Win32_Process ... Stop-Process`로 종료할 것
- 커밋 범위: 코드 전체(뷰·i18n·engine·scripts·server-mock 등) + cve-db.json + 테스트 zip. 제외: fsi_config.ini(시크릿), 산출물 *.xlsx/*.png
