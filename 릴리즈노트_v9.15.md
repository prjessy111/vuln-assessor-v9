# ADV (AI Diagnosis Verify) — 릴리즈 노트 v9.15

**릴리스일**: 2026-07-22
**이전 버전**: v9.14
**핵심**: 임의 스크립트 원격 점검 통일 + 결과 판정·리포트 + servers.csv 비번 암호화 + 소개/배포 정비

---

## 1. 원격 점검 경로 통일 (에이전트/결과물) ⭐
- `runOnTarget`: 결과파일 산출형(`result_glob`) 스크립트를 검증된 **`scriptDeployService`**(네이티브 `Invoke-Command`+base64 청크)로 위임 — Windows(WinRM)/Linux(SSH) 자동 분기, 대용량 결과 회수 문제 해소.
- 자격증명·IP를 **servers.csv 실시간 조회**로 결정(MySQL stale 저장소 회피), 도메인 계정 지원.
- **진행상태 UI**: 백그라운드 실행 + 진행바 오버레이(폴링).
- **배포 .ps1 UTF-8 BOM 자동부착** — 한글 스크립트가 원격 PowerShell 5.1(cp949 오독)에서 깨지던 문제(AD `ad_collect` 97행 파싱오류) 해결. (붙여넣기/파일 모두 정상)
- 임의 스크립트에 `-OutputDir` 강제주입 금지(`noOutputDirArg`) — 스크립트별 인자명 상이 대응.

## 2. 결과 판정·리포트 (구 "스크립트 즉시 점검" 개칭)
- 메뉴/화면 **"결과 판정·리포트"**(한/영/일/중) — 결과물 업로드/붙여넣기 → 판정(전용 evaluator/mock/로컬 LLM) → 리포트.
- **리포트 재사용**: judgment→`diagnoses` 어댑터(`agentReportAdapter`)로 기존 `/reports`(FSI·삼성 HTML + XLSX + 브라우저 인쇄 PDF) 그대로 사용. 자율 에이전트에도 "📄 리포트 생성" 버튼.
- AD 판정 = **결정적 규칙** `evaluateAdDc`(20여 항목, 수집/판정 분리 — 스크립트는 원시값만).

## 3. 보안 — servers.csv 비밀번호 암호화 🔐
- 비번 **AES-256-GCM** 저장(`enc:...`), 앱이 **자동 복호화**(평문 하위호환).
- 툴 `npm run enc-servers` — 평문→enc 1회 변환(바이트안전·idempotent).
- 키 = `.env` `ENCRYPTION_KEY`(src/config 로드). `key*.txt`/servers.csv/.env gitignore.
- servers.csv 단독 유출 시 비번 안전(키 없으면 복호화 불가).

## 4. 소개 페이지 · 배포 정비
- **소개(/intro)** 신규 — 강점(망분리 로컬 판정·노하우 판정규칙·국내 산출물) 어필 + 핵심기능·지원대상.
- **배포 가이드**(`배포_가이드.md`) + **배포 패키지 빌드**(`npm run build-deploy` → `dist/adv-deploy-package.zip`, 민감파일 제외) + 다운로드(`/download/deploy-package`).
- **신규 서버 연동 체크리스트**·**사전작업** 갱신(WinRM TrustedHosts, AD=DC 로컬+오프라인, 비번 암호화 절차).

## 5. 문서/회고
- 세션 핸드오프(`세션핸드오프_2026-07-22_WINRM.md`): AD 근본원인(BOM)·판정로직=노하우·지연분석·ADV 강평/상품화 평가.

---
### 참고 — 알려진 제약
- **AD DC 원격 자동 실행**: `Get-AD*`는 로컬 실행 정상이나 원격은 환경(더블홉/인코딩) 민감 → DC 로컬 실행 + 결과 판정·리포트(오프라인) 권장.
- 원격 자동 점검은 대상에 WinRM(5985)/SSH(22) 개방 필요 — 통제망에선 오프라인 경로 권장.
