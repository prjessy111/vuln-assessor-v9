# 세션 핸드오프 — 2026-07-22 (WinRM 원격 점검 통일 · 신규서버 가이드)

## 1. 이번에 해결/완료

### WinRM 원격 점검 경로 통일 (핵심)
- **문제:** 스크립트 즉시 점검(에드혹)/에이전트로 Windows(특히 AD DC `ad_collect.ps1`)를 **원격** 점검하려 했으나 실패.
  화면 에러 = `WinRM PowerShell execution failed: Failed to process the request, status Code:` (winrm_에러1.PNG).
- **정체:** 포트(5985)는 열렸는데 **권한(승인) 안 됨** = 대개 **컨트롤러 TrustedHosts 누락**. (107이 되는 이유 = 이 등록이 되어 있어서)
- **해결:** `src/agent/pipeline.js` `runOnTarget` 에서 **`result_glob`(결과파일 산출형) 스크립트는 검증된 `scriptDeployService.runDirectScriptDeployment` 경로로 위임**.
  - Windows: 기본 `powershell-only` = **네이티브 Invoke-Command(Negotiate)** — 107/fsi 에서 검증된 그 경로.
  - 결과 회수: **base64 청크**(`SCRIPT_DEPLOY_WINRM_CHUNK_SIZE`) → 예전 `-Raw` 대용량 유실 문제 **동시 해소**.
  - Linux 는 동일 함수가 SSH 로 분기.
- **강제승인 보강:** 운영자 직접입력(manual) 스크립트가 안전게이트 blocked(예: ad_collect 의 Remove-Item)라도, 게이트1에서 `force` 승인하면 `runOnTarget` 실행 재검사를 통과(`approval.forced` + `generated_by==='manual'` 조건). AI 생성 스크립트는 계속 차단.
- **검증:** `scripts/mock-verify/adhoc-pipeline-selftest.js` — WinRM 전송만 stub, 나머지 전부 실제 코드.
  실제 `JESSY-PC_AD_DC.txt` 로 provideScript→강제승인→runOnTarget→위임opts→구조판정 **전 구간 통과**(21개: 취약9·양호11·기타1, `structured:ad_dc`).
  result_glob 자동감지(`C:\ad_audit\*_AD_DC.txt`) → dir/pattern 분리(remoteResultDir=`C:\ad_audit`, resultGlob=`*_AD_DC.txt`)도 확인.
- **미검증(남은 1개):** **실제 110.91(DC) 대상 라이브 WinRM 홉** — 컨트롤러 TrustedHosts 등록 후 실행만 하면 됨(코드/판정은 검증 끝).

### 신규 서버 연동 가이드
- **`신규서버_연동_체크리스트.md`** (신규) — servers.csv 등록 후 Linux/Windows 각각 할 일 한 장 요약 + **컨트롤러 PowerShell 복붙 명령** + 에러조치표.
- **`사전작업.md`** — "참고(접속경로)" 섹션을 최신 사실로 수정(에이전트도 scriptDeployService 네이티브 경로 사용).
- **앱 도움말**(`/help`, `src/views/help/index.ejs`) — Windows=WinRM 원격 지원/AD 원격 점검으로 갱신, OS 요약표 갱신, **신규 서버 체크리스트 박스 + 컨트롤러 PowerShell 복붙 블록** 추가.

## 2. ★ 반드시 해야 할 향후 과제

### (A) servers.csv 암호화 — ✅ 완료 (v9.15)
- 비번 **AES-256-GCM**(`enc:...`) 저장 + `getTargetServersFromFile` 자동 복호화(평문 하위호환). 키=`.env ENCRYPTION_KEY`(src/config 로드).
- 툴 `npm run enc-servers`(scripts/encrypt-servers-csv.js) — 평문→enc 1회, 바이트안전·idempotent. `key*.txt`도 gitignore 추가.
- 검증: 5서버 round-trip + 207 실제 SSH 정상. 한계: servers.csv 단독유출=안전, .env(키)까지 함께 유출 시 복호화 가능 → 두 파일 분리·파일권한 최소화(가이드 명시).

### (A-2) 소개/배포/버전 — ✅ 완료 (v9.15)
- 소개 페이지 `/intro`(강점 어필), 배포 가이드 `배포_가이드.md`(MySQL 불필요=mock 모드, 파일목록, zip 해제→실행 절차), 배포 zip 빌드 `npm run build-deploy`→`/download/deploy-package`(민감파일 제외 검증), 버전 9.15.0 + `릴리즈노트_v9.15.md`.

### (B) 즉시 점검 리포트 (html/xlsx/pdf) — ✅ 완료
- **새로 안 만들고 기존 리포트 재사용.** `src/engine/agentReportAdapter.js`가 judgment→`diagnoses`(diagnose_type='llm') 변환 → 기존 `/reports`(fsi/samsung/policy3 HTML + XLSX + **인쇄PDF**) 그대로 사용.
- 라우트 `POST /agent/items/:id/to-report`, 화면 "📄 리포트 생성" 버튼. 단일(passwd)·다항목(AD) 변환 검증 완료.

### (C) 라이브 검증 — 부분 완료
- **리눅스 207 실검증 ✅**: `cat /etc/passwd` 원격 SSH 실행→수집(44줄)→로컬LLM(qwen) 판정(양호) end-to-end 통과.
- **Windows 110.91(DC) ✅ 접속·인증·배포·실행까지** 확인. 단 **AD cmdlet은 WinRM 더블홉으로 실패** → DC 로컬 실행+오프라인(수집결과 판정) 경로가 정답(도움말 반영).

## 3. 커밋/푸시 — ✅ 완료
- **HEAD=85cdd94, origin/main push 완료**(prjessy111). 코드·i18n·문서만 커밋, 민감/바이너리(xlsx·PNG·zip·result.txt·`*_AD_DC.txt`)는 제외.
- 남은 미커밋: 이 핸드오프의 최종 업데이트분(다음 커밋에 포함).
- **유일한 실질 미결 과제 = servers.csv 비번 암호화**(§2-A).

## 4. 회고 — 오늘 왜 오래 걸렸나 (정직하게)

### 가장 큰 원인: 검증된 코드를 안 쓰고 재발명 (내 잘못)
- 이미 107/fsi에서 동작하는 `scriptDeployService`(네이티브 Invoke-Command + base64 청크)가 있었는데,
  `scriptRunner`에 WinRM을 **처음부터 새로 구현**하다 대용량 결과 회수 버그에 빠져 오래 헤맴.
  사용자가 반복해서 "왜 그걸(기존 걸) 안 쓰냐"고 했고, 결국 위임으로 바꾸니 **인증·대용량 문제 동시 해결**.
- 교훈: **기존 검증 경로부터 재사용**. 새로 짜기 전에 "이거 이미 있나?" 먼저.

### 두 번째: 에러가 양파처럼 겹겹 (매 수정이 다음 에러를 드러냄)
순서대로: `password 없음`(mock 저장소에 비번 미저장) → `access denied`(로컬계정 UAC 토큰 필터링)
→ 도메인계정 전환 후에도 `access denied`(실은 **낡은 IP 100.91**로 접속 — MySQL 모드라 CSV 동기화 생략된 stale 저장소)
→ IP/계정 CSV 실시간 사용으로 수정 → `SyntaxError <`(클라 multipart 전송인데 파서 없어 HTML 리다이렉트)
→ urlencoded로 수정 → 접속 성공 → **AD cmdlet 더블홉**(근본 제약).
- 특히 `access denied`가 **두 원인(UAC + 낡은 IP)이 겹쳐** 서로를 가림 → 진단이 길어짐.

### 세 번째: 실환경 검증은 사용자 왕복이 필수
- WinRM/AD 문제는 실제 DC에서만 확인 가능 → 매 가설마다 "이 PowerShell 돌려주세요/스크린샷" 왕복 → 지연.
- 교훈: **결정적 진단 1개를 초반에** (whoami/groups + write test 한 줄) 돌려 원인을 한 번에 확정. 가설 순차 시도 금지.

### 놓친 환경 특성
- 앱이 **MySQL 모드**라 `seedMockData`(CSV→서버 동기화)가 생략됨 → 저장소 값이 낡음. 이걸 늦게 인지.
- 교훈: **저장소 모드(mock/MySQL)부터 확인**. 데이터 불일치 의심 시 소스(CSV) vs 저장소 대조.

### 분산된 병렬 요청
- 디버깅 중 i18n 이름변경·진행바 UI·리포트·실무성 질문이 섞여 초점이 분산.

## 5. 다음 작업 (결정됨)
- **리포트: 신규 제작 금지 — 재사용.** 에이전트/수집결과 판정의 judgment(findings)를
  기존 `diagnoses` 레코드 형식으로 변환하는 **어댑터**만 만들어 기존 `/reports`(fsi/samsung/policy3 HTML + XLSX + 인쇄PDF)를 그대로 태운다.
  - 참고: `buildReportFromAi(diag, server)` 가 `diag.results[]`(chk_id/status 등) + 카운트를 기대.
    findings({item,verdict,reason,evidence}) → results 매핑 + total/vuln/safe 카운트 채우면 됨.
  - PDF는 라이브러리 없이 **브라우저 인쇄**(사용자 확정). → **완료**(agentReportAdapter + `POST /agent/items/:id/to-report` + "📄 리포트 생성" 버튼).

## 6. AD 원격 점검 — ✅ 해결 (근본원인 = BOM 인코딩)
증상: 앱으로 110.91(DC) AD 점검 시 `WinRM ...:97 문자:57 NativeCommandError`. **더블홉 아니었음**(제가 컨트롤러에서 `Invoke-Command -FilePath`로 원격 실행 시 34KB 정상 회수 — AD cmdlet은 원격에서 잘 됨).

**진짜 원인 = BOM 누락:** 붙여넣은 UTF-8 스크립트에 BOM이 없어 원격 **PowerShell 5.1이 파일을 cp949(ANSI)로 오독** → ad_collect의 한글("…명" 등)이 깨져 97행 파싱실패. (파일 업로드는 repo 파일에 BOM이 있어 됐고, 붙여넣기만 BOM 없어 깨짐.)

**수정(커밋됨):**
- `pipeline._runViaScriptDeploy`: 배포 .ps1에 **UTF-8 BOM 자동부착**(이미 있으면 중복 금지, .sh는 미부착). → 110.91 A/B 실검증: BOM없음 실패(97행)/BOM추가 성공(34KB).
- `scriptDeployService.noOutputDirArg`: 임의 스크립트에 `-OutputDir` 강제주입 금지(ad_collect은 `-OutDir`) — 확장성 버그도 함께 수정.
- 실행경로: mock 저장소가 아닌 **servers.csv 실시간 조회**로 IP·계정 결정(MySQL stale 회피), 도메인계정 지원.

**결론:** AD 원격 정상 동작. 실행 #3182로 리포트까지 확인(취약9·양호11·불가1). **한글 스크립트는 파일 업로드 권장(또는 앱이 BOM 자동부착).**

## 7. ADV 강평 (오늘 사용자 요청 — 정직 버전)
**장점:** ① 망분리 로컬 LLM 판정(외부 API 없이 — 국내 금융·공공 각) ② 국내 컴플라이언스 산출물(FSI/금보원/삼성)+SecuMS 3-way 정합성 ③ 분석가 판정 공수 절감 ④ 확장 구조(evaluator 레지스트리).
**단점:** ① 원격 실행 PoC급(WinRM 세팅지옥·AD 더블홉·오늘 하루가 증거) ② **보안 위생 미달(servers.csv 평문 비번)** — 보안제품 아이러니, 도입심사 탈락요인 ③ 판정 정확도 대규모 실증 부족(핵심 주장인데) ④ 통합 이음새 약함(오늘 stale저장소·cred·multipart·인자주입 연쇄) ⑤ IP 해자 약함.
**상품화 결론:**
- 지금 이대로 **독립 상용 제품은 무리**(보안위생·정확도입증·인증·안정성 미충족).
- **현실 경로:** C.내부도구/컨설팅 가속기(지금도 가치) → 정확도·레퍼런스 축적 → B.**부가 모듈**("기존 스캐너 결과에 AI판정+리포트 자동화 레이어", 대체 아닌 보완)이 가장 현실적.
- 포지셔닝을 **"수집무관 판정·리포트 엔진"**으로. "종합 취약점 플랫폼" 과대포장 금지.
**상품화 전 필수:** ① 자격증명 암호화 ② 판정 정확도/오탐율 실측(제3자 정답지, 규모) ③ 원격 강등·오프라인 메인화·회귀테스트 ④ 포지셔닝 명확화.
**한 줄:** "기술 각(망분리 AI판정+국내 산출물)은 진짜. 지금은 내부도구/가속기 단계. 보안위생+정확도 입증 후 부가모듈로 상품화가 현실적 길."

## 8. 판정 로직 = 핵심 자산 (노하우) — ★중요
- **수집과 판정 분리 설계.** 스크립트(ad_collect 등)는 **원시 값만** 수집(read-only, no verdict). **판정 지능은 우리 코드가 소유.**
- AD 판정 = `src/engine/adhocJudge.js`의 **`evaluateAdDc`(손코딩 규칙 20여 개)** — KISA/AD 보안기준을 코드화(예: `MinPasswordLength<8→취약`, `PASSWD_NOTREQD 플래그→취약`, `SMB1 활성→취약`, `Domain Admins>5→취약`, `WDigest=1→취약`). **결정적·LLM 무관**(mock 백엔드, `structured:ad_dc`).
- **이번 AD 판정은 LLM 아니라 이 노하우 규칙이 함.** LLM은 "규칙 없는 임의 스크립트"용 보조일 뿐, 신뢰도는 규칙이 위.
- **판정 3방법(스크립트마다 코딩 불필요):**
  1. **전용 evaluator**(구조 판정기) — 코딩 필요, 최고 정확도. 반복·중요 형식만 선택적으로.
  2. **mock 시그니처**(RISK_SIGNALS) — 코딩 0, 흔한 위험만.
  3. **로컬 LLM + 판정 지침(자연어)** — 코딩 0, 임의 스크립트, 정확도 모델 의존.
  → 전략: **핵심 반복점검=evaluator, 나머지=LLM+지침.** evaluator 레지스트리(`SCRIPT_EVALUATORS`)로 승격 확장.

## 9. AD 점검이 왜 오래 걸렸나 (양파 7층 — 표면 에러가 진짜 원인 가림)
1 `Failed to process the request`=TrustedHosts → 2 `password 없음`=mock 저장소 비번 미저장(CSV 조회로 해결) → 3 `access denied`=로컬계정 UAC 필터링(도메인계정) → 4 여전히 실패=**낡은 IP 100.91**(MySQL stale, CSV 진실로 해결) → 5 `Unexpected token <`=fetch multipart→HTML(urlencoded) → 6 `-OutputDir` 강제주입 불일치(noOutputDirArg) → 7 **진짜=BOM 누락**(한글 cp949 오독). 
- 지연 핵심: 표면 6개가 근본(BOM)을 가림 + 실환경 왕복 + 내 삽질(scriptDeployService 재발명, BOM A/B 오판). 교훈: 한글 스크립트=파일업로드, 결정적 진단 1개 초반에, 검증경로 재사용, 저장소모드(MySQL/mock) 먼저 확인.

## 10. ADV 핵심 기능 (사용자 정리 확인)
사용자 제시 3대 핵심 = **대체로 맞음**, 아래로 정리:
1. **결과 판정·리포트**(오프라인): 결과물 업로드/붙여넣기 → 판정(evaluator/mock/LLM) → 기존 리포트(HTML/XLSX/인쇄PDF).
2. **자율 진단 에이전트**(원격): 자연어→스크립트 생성 **또는** 스크립트 직접입력/업로드 → WinRM/SSH 원격 실행 → 판정 → 리포트.
3. **3-way 정합성 검증**: SecuMS(정답지) vs AI vs LLM 교차검증(신뢰도).
- 그 밑을 받치는 **진짜 코어(해자)** = ⓐ **수집/판정 분리 + 노하우 판정규칙(evaluator)**, ⓑ **망분리 로컬 판정**(mock/사내 LLM, 외부 API 0), ⓒ **국내 컴플라이언스 산출물**.
- (참고: 서버 정기점검/수집·조치관리·감사로그·예외처리·i18n 등은 지원 기능. 차별점은 위 3+코어.)
