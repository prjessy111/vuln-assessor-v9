# ADV 배포 가이드 (다른 서버에 설치)

> 다른 PC/서버에 ADV 를 설치·구동하는 데 필요한 파일과 절차. 다음에 이것만 보면 됨.

---

## 0. 요약 — MySQL 필요한가?
**아니요. 필수 아닙니다.** ADV 는 두 모드 지원:
- **mock 모드(기본)** — 로컬 JSON 파일(`data/mock/`)에 저장. **MySQL 불필요.** `npm run mock` 만으로 동작.
- **mysql 모드(선택)** — 다중 사용자·영속성 필요 시. `.env` 에 `DB_MODE=mysql` + DB 접속정보. (MySQL 설치·스키마 필요)

→ **처음/단독/PoC 는 mock 모드로 충분.** MySQL 없이 바로 됩니다.

---

## 1. 필요한 것 (런타임)
- **Node.js** (LTS). `npm install` 로 의존성 설치.
- 실행: `npm run mock` → `http://<host>:3000`
- (선택) **로컬 LLM 게이트웨이**(LSAP/ollama) — LLM 판정 쓸 때만. 없으면 mock/결정적 판정만.
- (원격 점검 시) 대상에 **WinRM 5985(Windows) / SSH 22(Linux)** 개방 + 컨트롤러 TrustedHosts(→ 사전작업.md).

## 2. 배포에 포함할 파일 (소스)
| 항목 | 내용 |
|---|---|
| `server-mock.js` | 앱 진입점 |
| `src/` | 전체 로직(엔진·에이전트·뷰·i18n·유틸) |
| `package.json` (+lock) | 의존성 정의 |
| `rules/` | 룰셋 |
| `data/policies/`, `data/cve/cve-db.json`, `data/srv-2026-scope.json` | 판정 기준·CVE·범위 |
| `scripts/` | 수집 스크립트(ai-ready), 유틸 스크립트 |
| `ad_collect.ps1` | AD 수집 스크립트 |
| 가이드 MD | 사전작업·신규서버연동·배포·릴리즈노트 |

## 3. 배포에서 **제외**(대상에서 새로 설정) — 민감/런타임
| 파일 | 처리 |
|---|---|
| `.env` | **대상에서 새로 작성** (`.env.example` 참고). `ENCRYPTION_KEY`·LLM·(DB) |
| `key*.txt` | Claude 키 등 — 필요 시 대상에서 |
| `servers.csv` | 대상 목록 — 새로 작성 후 `npm run enc-servers` (`servers.csv.example` 참고) |
| `node_modules/` | `npm install` 로 재생성 |
| `data/mock/`, `data/uploads/` | 런타임 데이터 — 자동 생성/시드 |

## 4. 배포 절차
1. **패키지 받기**: 소스 zip (아래 5번) 또는 git clone.
2. `npm install`
3. `.env` 작성 (`.env.example` 복사 후 값 채움) — 최소 `ENCRYPTION_KEY`, LLM 설정.
   - 키 생성: `node -e "console.log(require('./src/util/crypto').generateMasterKey())"`
4. `servers.csv` 작성(평문) → `npm run enc-servers` (비번 암호화).
5. `npm run mock` → `http://<host>:3000`. (MySQL 모드면 `.env DB_MODE=mysql` + DB 준비)
6. (원격 점검) 대상 WinRM/SSH 개방 + 컨트롤러 TrustedHosts.

## 5. 배포 패키지 zip (자동 빌드)
민감파일을 제외한 소스 zip 을 자동 생성:
```
npm run build-deploy      # → dist/adv-deploy-package.zip
```
- 제외: `.env`·`key*.txt`·`servers.csv`·`node_modules`·`data/mock|uploads`·xlsx·png·bak 등.
- 포함: 코드 + `.env.example` + `servers.csv.example`.
- **다운로드**: 앱 실행 후 **`/download/deploy-package`**(소개 페이지 링크) 또는 `dist/adv-deploy-package.zip` 직접 전달.

> 🔒 **배포 zip 은 비밀번호로 암호화**되어 있습니다 — **비번은 별도 전달**받은 값. Windows 기본 탐색기/`Expand-Archive`는 암호 zip 을 못 풀므로 **7-Zip · WinRAR 등**으로 해제하세요. (빌드 시 비번은 `.env` 의 `DEPLOY_ZIP_PASSWORD`)

### 5-1. 받은 zip 으로 대상 서버에서 실행 (압축 해제 후)
```bash
# 1) 압축 풀기 — 비번은 별도 전달값 (7-Zip/WinRAR 등)  <비번> 자리에 입력
#    Windows(7-Zip):  7z x adv-deploy-package.zip -p<비번> -oC:\adv
#    Windows(WinRAR): 우클릭 "압축 풀기" → 비번 입력
#    Linux:           unzip -P <비번> adv-deploy-package.zip -d ~/adv
cd C:\adv           # (푼 폴더로 이동)

# 2) 의존성 설치
npm install

# 3) 환경설정: .env.example → .env 로 복사 후 값 채우기
#    (Windows) copy .env.example .env     (Linux) cp .env.example .env
#    - ENCRYPTION_KEY 생성:
node -e "console.log(require('./src/util/crypto').generateMasterKey())"
#      출력값을 .env 의 ENCRYPTION_KEY= 에 붙여넣기
#    - LLM_ENDPOINT / LLM_API_KEY / LLM_MODEL 채우기 (없으면 mock 판정만)

# 4) 대상 목록: servers.csv.example → servers.csv (평문 비번) 후 암호화
#    (Windows) copy servers.csv.example servers.csv    (Linux) cp ...
npm run enc-servers        # 평문 → enc: 암호화

# 5) 실행 (MySQL 불필요 = mock 모드)
npm run mock               # → http://localhost:3000
```
- MySQL 모드로 쓸 때만: `.env` 에 `DB_MODE=mysql` + `DB_*` 채우고 MySQL/스키마 준비.
- 원격 점검하려면: 대상에 WinRM(5985)/SSH(22) 개방 + 컨트롤러 TrustedHosts(→ 사전작업.md).
- 소개 페이지(`/intro`)·도움말(`/help`)에서 기능·연동 절차 확인.

## 6. 보안 주의
- `.env`(키)·`servers.csv`·`key*.txt` 는 **gitignore**. 배포 zip 에도 미포함.
- servers.csv 비번은 암호화(`enc:`)되지만, **키(.env)와 함께 유출되면 복호화 가능** → 두 파일 분리 관리·파일권한 최소화.
- 상세: 신규서버_연동_체크리스트.md(🔐 비밀번호 암호화), 세션핸드오프_2026-07-22_WINRM.md(§2 보안).
