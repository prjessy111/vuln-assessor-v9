# ── 의존성 ──
node_modules/

# ── 민감정보 (절대 커밋 금지) ──
key.txt
.env
.env.*
servers.csv
*.pem
*.key
*token*

# ── 런타임/수집 데이터 (자격증명·비번 해시·서버 raw 포함) ──
data/mock/
data/uploads/
*.db
*.sqlite

# ── 백업·로그 ──
*.bak
*.bak-*
*.log

# ── 대용량 미디어 (소스 아님 — 동영상은 별도 전달) ──
*.mp4
*.mov
*.webm
*.mkv

# ── OS ──
.DS_Store
Thumbs.db
