# ── 의존성 ──
node_modules/

# ── 민감정보 (절대 커밋 금지) ──
key.txt
.env
.env.*
servers.csv
*.pem
*.key
*token*
!src/agent/token.js
# DB 접속설정(평문 자격증명 — sa 비번 등) 커밋 금지
scripts/ai-ready/fsi_config.ini
fsi_config.ini
# AD 수집 실결과(도메인·IP·계정 등 내부정보) 커밋 금지
*_AD_DC.txt
# VPS/Hermes 접속·점검 메모 (IP·키·경로 등 — 커밋 금지)
info.txt
info*.txt
*.vps
data/agent-credentials.json
hermes_key*
id_rsa*
id_ed25519*

# ── 런타임/수집 데이터 (자격증명·비번 해시·서버 raw 포함) ──
data/mock/
data/uploads/
*.db
*.sqlite

# ── 백업·로그 ──
*.bak
*.bak-*
*.log

# ── 대용량 미디어 (소스 아님 — 동영상은 별도 전달) ──
*.mp4
*.mov
*.webm
*.mkv

# ── OS ──
.DS_Store
Thumbs.db

# 로컬 설정(토큰 포함) — 커밋 금지
.claude/settings.local.json
