# 사전작업 — Windows 대상 서버 WinRM 원격 진단 준비

> vuln-assessor 가 Windows 서버를 원격 진단하려면 **WinRM** 접속이 열려 있어야 한다.
> 이 문서는 **앱이 도는 PC(컨트롤러)** 에서만 설정해 대상 서버에 접속을 여는 절차다.
> (대상 서버에 WinRM 이 이미 켜져 있는 경우 — 컨트롤러 설정만으로 완료된다.)

---

## 0. 개념 정리

| 구분 | 역할 | 이 문서에서 건드리는가 |
|---|---|---|
| **컨트롤러 PC** | vuln-assessor(node) 앱이 도는 PC. 접속을 *거는* 쪽 | ✅ 여기서만 작업 |
| **대상 서버** | 진단 대상 Windows 서버 (예: jessy107 / 192.168.159.107) | ❌ 손대지 않음 (WinRM 5985 만 열려 있으면 됨) |

- 접속 방식: **WinRM / HTTP 5985**
- 인증: 컨트롤러의 `TrustedHosts` 에 대상 IP 등록 필요
- `TrustedHosts` 는 **접속을 거는 쪽(컨트롤러)** 에 설정해야 효과가 있다.

---

## 1. 관리자 권한 PowerShell 열기

- **Win + X** → **Windows PowerShell(관리자)** / **터미널(관리자)**
- 또는 시작 → `powershell` → 우클릭 → **관리자 권한으로 실행**
- 창 제목이 **"관리자: Windows PowerShell"**, 프롬프트가 `C:\WINDOWS\system32>` 인지 확인

> ⚠️ 일반 창에서 실행하면 `Access is denied` / `WinRM 서비스를 열 수 없습니다` 오류가 난다. 반드시 관리자 창에서.

---

## 2. 컨트롤러의 WinRM 클라이언트 서비스 켜기

```powershell
Start-Service WinRM
Set-Service WinRM -StartupType Automatic
```

- 메시지 없이 넘어가거나 "이미 실행 중" → 정상

---

## 3. 대상 서버를 신뢰 대상(TrustedHosts)에 등록

```powershell
Set-Item WSMan:\localhost\Client\TrustedHosts -Value "192.168.159.107" -Concatenate -Force
Restart-Service WinRM
```

- 여러 대면 쉼표로: `-Value "192.168.159.167,192.168.159.107"`
- `-Concatenate` 덕분에 기존 등록은 유지하고 추가된다.
- `Restart-Service` 시 "중지 대기 중" 경고가 한두 번 떠도 정상. 10초쯤 기다리면 끝남.

**등록 확인:**
```powershell
Get-Item WSMan:\localhost\Client\TrustedHosts
```
→ `Value` 칸에 대상 IP 들이 보이면 OK.

---

## 4. 네트워크 연결 확인

```powershell
Test-NetConnection 192.168.159.107 -Port 5985
```

- **`TcpTestSucceeded : True`** → 5단계로
- **`False`** → 대상 서버의 WinRM 이 꺼져 있거나 방화벽 차단 (→ 아래 "대상 서버가 막힌 경우" 참고)

---

## 5. 실제 접속 테스트

```powershell
$cred = Get-Credential
```
팝업 입력:
- 사용자 이름: `Administrator`
- 암호: `omni007!`  *(servers.csv 의 해당 서버 비밀번호)*

```powershell
Invoke-Command -ComputerName 192.168.159.107 -Credential $cred -ScriptBlock { hostname; whoami }
```

- 대상의 **hostname + `대상\administrator`** 가 출력되면 → **성공** 🎉
- hostname 만 떠도 인증·실행이 통과한 것 (= 성공)
- 빨간 에러면 → 아래 "자주 나는 에러" 참고

---

## 6. 마무리

1. **vuln-assessor(node) 앱 재시작** — 새 WinRM 설정 반영
2. 앱의 **서버 관리 / 진단** 화면에서 대상 서버 수집·진단 실행
3. 스크립트 배포·실행 → 결과 XML 수신 확인

---

## 부록 A — 자주 나는 에러

| 에러 | 원인 | 조치 |
|---|---|---|
| `Access is denied` (서비스 제어 시) | PowerShell 이 관리자 아님 | 관리자 창으로 다시 열기 (1단계) |
| `WinRM client cannot process the request` / `신뢰` | TrustedHosts 미등록 | 3단계 재확인 |
| `Access denied` (Invoke-Command 시) | 계정/비번 오류 또는 대상 인증정책 | 비번 확인, 대상 Basic 인증(부록 B) |
| 연결 timeout / `TcpTestSucceeded: False` | 대상 WinRM 미기동 또는 방화벽 5985 차단 | 부록 B |

---

## 부록 B — 대상 서버가 막힌 경우 (4단계가 False)

대상 서버에 직접 로그인(RDP) 없이, **컨트롤러에서 SMB(445)로 원격으로 WinRM 을 켜는** 방법.
[Sysinternals PsExec](https://learn.microsoft.com/sysinternals/downloads/psexec) 필요.

```powershell
psexec \\192.168.159.107 -u Administrator -p omni007! -h `
  powershell -Command "Enable-PSRemoting -Force; winrm set winrm/config/service/auth '@{Basic=\"true\"}'; winrm set winrm/config/service '@{AllowUnencrypted=\"true\"}'"
```

- 대상에 **admin SMB 접근(445)** 이 열려 있어야 동작.
- SMB 도 막혀 있으면 → 대상 서버에 한 번은 직접 접속해 WinRM 을 켜야 한다.

> ⚠️ `Basic` + `AllowUnencrypted` 는 **사내 폐쇄망 PoC 기준**. 외부망이면 HTTPS(5986)+Negotiate 로 가야 한다.

---

## 참고 — 접속 경로 (코드 기준, 2026-07 통일)

- **결과파일 산출형 점검**(에드혹/에이전트에서 `result_glob` 지정): `src/agent/pipeline.js` → `runOnTarget` → **`scriptDeployService.runDirectScriptDeployment`** 로 위임.
  - Windows: 기본 **`powershell-only`(네이티브 Invoke-Command / Negotiate)** → 컨트롤러 **TrustedHosts** 필요, 대상 Basic 불필요.
  - Linux: **SSH**.
  - 결과 회수: **base64 청크**(`SCRIPT_DEPLOY_WINRM_CHUNK_SIZE`) → 대용량 결과파일도 유실 없이 회수.
- **stdout 캡처형 점검**(결과파일 없음): 기존 `src/agent/scriptRunner.js` 경로.

> 5단계 `Invoke-Command` 테스트가 통과하면 앱의 기본 경로도 그대로 붙는다(같은 방식).
> 예외적으로 Negotiate 가 막힌 폐쇄망이면 환경변수 `SCRIPT_DEPLOY_WINDOWS_WINRM_MODE=nodejs-winrm` 로 Basic(5985) 경로 전환 가능 — 이때만 대상에 `Basic`/`AllowUnencrypted`(부록 B) 필요.

---

## servers.csv 예시

```
# hostname,ip,os,username,password[,asset_no][,server_id][,transport]
jessy167,192.168.159.167,windows,Administrator,omni007!,SVR-2026-0167,99
jessy207,192.168.159.207,linux,root,1q2w3e4r!,SVR-2026-0030,50
jessy107,192.168.159.107,windows,Administrator,omni007!,SVR-2026-0031,60
# transport(8번째 컬럼, 선택): Windows 원격 방식 winrm(기본) | ssh(OpenSSH)
JESSY-PC,192.168.110.91,windows,JESSYDOMAIN\Administrator,omni007!,SVR-2026-0033,62,ssh
```

---

## 부록 C — Windows 대상 OpenSSH 방식 (WinRM 대안, 2026-07-28 추가)

Windows 대상도 **WinRM 대신 SSH(OpenSSH Server)** 로 점검할 수 있다. 선택 방법(우선순위 순):

1. **수집 화면**: 배포 폼의 "Windows 원격 방식" 셀렉트 → `SSH (OpenSSH)` 선택 (일회성 오버라이드)
2. **servers.csv**: 8번째 컬럼에 `ssh`
3. **환경변수**: `.env` 에 `SCRIPT_DEPLOY_WINDOWS_TRANSPORT=ssh` (전체 기본값 변경)

미지정 시 기본은 **winrm** (기존 동작 그대로).

### 대상 서버에 OpenSSH Server 설치 (Windows Server 2019/2022, Windows 10 1809+)

대상 서버에서 **관리자 PowerShell**:

```powershell
# 1) 설치 (Features on Demand — 인터넷 또는 WSUS 필요)
Add-WindowsCapability -Online -Name OpenSSH.Server~~~~0.0.1.0

# 2) 서비스 시작 + 자동 시작
Start-Service sshd
Set-Service -Name sshd -StartupType 'Automatic'

# 3) 방화벽 22/tcp 허용 (설치 시 자동 생성되는 경우가 많음 — 없으면 추가)
if (-not (Get-NetFirewallRule -Name 'OpenSSH-Server-In-TCP' -ErrorAction SilentlyContinue)) {
  New-NetFirewallRule -Name 'OpenSSH-Server-In-TCP' -DisplayName 'OpenSSH Server (sshd)' `
    -Enabled True -Direction Inbound -Protocol TCP -Action Allow -LocalPort 22
}

# 4) 확인
Get-Service sshd
```

- 컨트롤러에서 확인: `Test-NetConnection <대상IP> -Port 22` → `ssh Administrator@<대상IP>` 로그인 테스트.
- OpenSSH 기본 셸은 **cmd.exe** — 앱이 `powershell -NoProfile -Command ...` 로 감싸 실행하므로 기본 셸 변경 불필요.
- 도메인 계정은 `JESSYDOMAIN\Administrator` 형식 그대로 사용 가능 (`ssh 'JESSYDOMAIN\Administrator'@<IP>`).
- 파일 전송은 SFTP(sshd 내장) 사용 — 추가 설정 불필요.

### WinRM 이 이미 되는 서버라면 — 원격으로 설치 가능

컨트롤러에서 (TrustedHosts 등록된 상태):

```powershell
$cred = Get-Credential   # 대상 서버 관리자 계정
Invoke-Command -ComputerName 192.168.110.91 -Credential $cred -ScriptBlock {
  Add-WindowsCapability -Online -Name OpenSSH.Server~~~~0.0.1.0
  Start-Service sshd
  Set-Service -Name sshd -StartupType 'Automatic'
}
```

### 구버전 Windows (Server 2016 이하 — Windows Capability 미지원)

GitHub 릴리스 zip 설치. **✔ 110.91(JESSY-PC, Server 2012 R2 DC)에 이 방식으로 OpenSSH 10.0p2 원격 설치·SSH 배포 검증 완료 (2026-07-28).**

> 주의: Server 2012 R2 는 PowerShell 4.0 — `Expand-Archive` 가 없어 .NET `ZipFile` 사용.

```powershell
# 1) zip 을 대상 서버로 복사 (인터넷 안 되면 SMB C$ 공유로 밀어넣기)
#    https://github.com/PowerShell/Win32-OpenSSH/releases/latest/download/OpenSSH-Win64.zip
#    → C:\Windows\Temp\OpenSSH-Win64.zip

# 2) 압축 해제 (PS 4.0 호환)
Add-Type -AssemblyName System.IO.Compression.FileSystem
[System.IO.Compression.ZipFile]::ExtractToDirectory('C:\Windows\Temp\OpenSSH-Win64.zip', 'C:\Program Files\OpenSSH')

# 3) 서비스 설치 + 시작 + 방화벽
& 'C:\Program Files\OpenSSH\OpenSSH-Win64\install-sshd.ps1'
Set-Service -Name sshd -StartupType 'Automatic'
Start-Service sshd
New-NetFirewallRule -Name 'OpenSSH-Server-In-TCP' -DisplayName 'OpenSSH Server (sshd)' -Enabled True `
  -Direction Inbound -Protocol TCP -Action Allow -LocalPort 22
```

위 전 과정은 WinRM 이 되는 서버라면 컨트롤러에서 `Invoke-Command` 로 원격 수행 가능 (110.91 실 사례 — zip 은 SMB C$ 로 복사).
