# ADV (AI Diagnosis Verify) — 릴리즈 노트 v9.16

**릴리스일**: 2026-07-28
**이전 버전**: v9.15
**핵심**: Windows 원격 점검 이중 전송(WinRM + SSH/OpenSSH) — 서버별·배포별 선택 가능

---

## 1. Windows 원격 점검 SSH(OpenSSH) 전송 지원 ⭐
- Windows 대상도 **WinRM 외에 SSH(OpenSSH Server)** 로 배포·실행·결과 회수 가능 — Linux 와 동일한 SSH/SFTP 경로 재사용.
- **선택 방법(우선순위 순)**, 미지정 시 기존 그대로 **winrm 기본**:
  1. 수집 화면 배포 폼 **"Windows 원격 방식"** 셀렉트(`remote_transport`) — 일회성 오버라이드
  2. **servers.csv 8번째 컬럼** `transport` = `winrm` | `ssh`
  3. 환경변수 `SCRIPT_DEPLOY_WINDOWS_TRANSPORT` — 전체 기본값 변경
- SSH 경로도 WinRM 과 **동일 규약**: 작업폴더(`C:\Windows\Temp\vuln-assessor-<job>`), `.ps1` 에 `-OutputDir` 자동 주입(`noOutputDirArg` 로 생략), 결과 XML 탐색·회수, 타임아웃 15분.
- Windows SSH 는 **키 인증도 지원**(`ssh_key_path`) — WinRM 과 달리 비밀번호 필수 아님.

## 2. 실측 검증 (2026-07-28)
- **110.91 (JESSY-PC, Windows Server 2012 R2 DC)** 에 OpenSSH Server **10.0p2** 원격 설치(WinRM 경유, zip 방식) 후:
- 새 SSH 전송으로 **배포 → .ps1 실행 → 결과 XML 회수** 엔드투엔드 성공 (`transport=ssh`).
- 회귀: jest **95/95 통과**, 에드혹 파이프라인 셀프테스트 통과.

## 3. Unix 수집 스크립트 — Ubuntu 호환 보강
- `fsi_unix_ai.sh`: **netstat/ifconfig 미설치 환경(Ubuntu 18.04+ 등) 폴백** — `ss` 출력을 netstat 컬럼 순서로 재배열(기존 포트 파서 호환), 인터페이스는 `ip -s link`/`ip addr` 대체. net-tools 있는 서버(CentOS 등)는 동작 불변.
- PAM 복잡도 점검(`CMD_PAM1~3`)에 Debian/Ubuntu 경로 `/etc/pam.d/common-password` 추가 (RHEL `system-auth`와 병행).

## 4. 가이드 갱신
- `사전작업.md` **부록 C** — Windows 대상 OpenSSH 설치·세팅:
  - Server 2019+/Win10 1809+: `Add-WindowsCapability`(Features on Demand)
  - **Server 2016 이하**(2012 R2 포함): GitHub `Win32-OpenSSH` zip 설치 (110.91 실 사례)
  - WinRM 이 이미 되는 서버는 **원격 설치 가능**(Invoke-Command) + 방화벽 22/tcp
- servers.csv 헤더 주석에 `transport` 컬럼 설명 추가.

---
### 참고 — 알려진 제약
- Windows SSH 전송은 대상에 **OpenSSH Server(22/tcp)** 필요 — 미설치 서버는 기본 WinRM 경로 그대로 사용.
- OpenSSH 기본 셸(cmd.exe) 기준으로 동작 — 대상 서버의 기본 셸 변경 불필요.
- Server 2012 R2 는 PowerShell 4.0 이라 `Expand-Archive`/`Add-WindowsCapability` 미지원 — 가이드의 zip 절차 사용.
