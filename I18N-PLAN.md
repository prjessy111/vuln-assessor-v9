# 다국어(i18n) 지원 계획 — 한국어/영어/일본어/중국어

작성: 2026-07-16

## 1. 목표

웹 UI를 4개 언어(ko/en/ja/zh)로 제공. 언어 전환은 헤더의 언어 선택기로 즉시 반영(쿠키 저장, 새로고침 유지).

## 2. 현재 규모 (측정값)

| 항목 | 값 |
|---|---|
| EJS 뷰 파일 | 62개 |
| 뷰 총 라인 | 10,195줄 |
| 한국어 포함 라인 | 약 2,765줄 |
| 서버측 한국어 문자열 | server-mock.js 등 (콘솔로그·알림 메시지) |

## 3. 번역 대상 / 비대상

**대상 (UI 크롬)**
- 네비게이션·버튼·테이블 헤더·폼 라벨·안내 문구·빈 상태 메시지
- 페이지 제목, 상태 뱃지(양호/취약/보류 등)

**비대상 (데이터 — 이번 범위 아님, 별도 과제)**
- DB 저장 데이터: 진단 항목명(SRV-xxx 제목), 점검 결과 raw, LLM 판정 사유문
- 엑셀 리포트 산출물 (ExcelJS 생성부)
- 수집 스크립트(sh/ps1) 출력
- 알림/감사로그에 이미 저장된 과거 메시지

→ 즉, 언어를 바꾸면 **화면 틀은 해당 언어, 진단 데이터 내용은 한국어 유지**. 데이터까지 번역하려면 LLM 실시간 번역 계층이 필요하며 이번 범위에서 제외.

## 4. 아키텍처

```
src/i18n/
  index.js                # 로더 + t() + Express 미들웨어
  locales/
    ko/common.json        # 네비·버튼·공통 라벨 (원본 기준)
    ko/dashboard.json     # 화면별 네임스페이스
    ko/diagnosis.json ...
    en/…  ja/…  zh/…      # 언어별 동일 구조
```

- **키 방식**: `t('common.nav.dashboard')` — `파일명.중첩키`로 플랫화
- **폴백**: 요청언어 → ko → 키 그대로 표시 (누락돼도 화면 안 깨짐)
- **언어 결정 우선순위**: `?lang=xx` 쿼리(쿠키 저장) → `lang` 쿠키 → Accept-Language → ko
- **뷰 사용법**: `<%= t('common.btn.save') %>`, 파라미터는 `{n}` 치환
- **스크립트 내 문자열**: 뷰 상단에서 `var I18N = <%- JSON.stringify({...}) %>` 주입
- 외부 라이브러리 불필요(자체 구현 ~80줄), 서버 기동 시 1회 로드

## 5. 단계별 계획·예상 소요

| 단계 | 내용 | 소요 |
|---|---|---|
| **1단계** | i18n 코어 + 미들웨어 + 언어 선택기 + 공통(헤더/푸터/네비) + 핵심 화면(로그인·대시보드) 4개 언어 | 이번 세션 즉시 (~30분) |
| **2단계** | 일반 화면 ~44개 뷰 전환 (병렬 에이전트로 디렉토리별 분담) | 1~2시간 (이번 세션 착수) |
| **3단계** | 리포트 뷰 12개(view_fsi 등 — 데이터 라벨 얽혀 신중 필요) + 서버측 알림 메시지 | 별도 세션 권장 |
| **(향후)** | 진단 데이터·LLM 판정문 실시간 번역, 엑셀 다국어 | 미정 — LLM 번역 계층 설계 필요 |

수작업이었다면 며칠 걸릴 규모(고유 문자열 수백 개 × 4개 언어)이나, 기계적 패턴이라 에이전트 병렬화로 단축.

## 6. 리스크·주의

- EJS 속성 안(`title="..."`, `placeholder="..."`)과 `<script>` 안 문자열은 이스케이프 컨텍스트가 달라 전환 시 실수 여지 → 화면별 기동 확인 필수
- `날짜/숫자 포맷`은 이번 범위에서 현행 유지 (YYYY-MM-DD는 언어 중립적)
- 번역 품질: 보안 용어(취약점/조치/예외/제외 등)는 용어집 고정 후 일괄 적용
  - 취약점 vulnerability / 脆弱性 / 漏洞
  - 조치 remediation / 対応(是正) / 整改
  - 예외 exception / 例外 / 例外
  - 진단 assessment / 診断 / 诊断
  - 수집 collection / 収集 / 采集

## 7. 완료 기준

- [ ] 언어 선택기에서 4개 언어 전환, 새로고침 후 유지
- [ ] 1·2단계 화면에서 한국어 하드코딩 잔존 없음 (grep 검증)
- [ ] ko 화면이 기존과 픽셀 동일 (회귀 없음)
- [ ] 서버 기동·주요 화면 렌더 스모크 테스트 통과
